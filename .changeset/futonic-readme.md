---
"futonic": patch
---

Add a package README covering the service-developer and host-developer workflow. The typesafe client is better-call's `createClient`, re-exported from `futonic/client` and typed from the service `router` (`client("@post/tickets", { body })`).
